import {Alert } from "@mui/material";

const NotFound = () => {
  return (
        <Alert  severity="warning"  sx={{fontSize:'1rem' ,mt:"10vh",mx:'30vw',opacity:.8}}>
          Not Found !
        </Alert>
    )
};

export default NotFound;
